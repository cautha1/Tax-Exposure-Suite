Main objective

Build a Python backend that makes all platform buttons functional.

I want real backend logic for:

login-related flows if needed
creating companies
uploading CSV files
importing transactions
running tax analysis
updating risk statuses
generating reports
fetching dashboard summaries
settings/profile updates
Product context

The platform is called:

Tax Exposure Intelligence Platform

Purpose:
Help advisory firms and finance teams detect tax exposure and compliance risk before audits happen.

Users:

admin
advisor
client_user

Supabase tables already exist or will exist:

profiles
companies
company_users
uploads
transactions
tax_risk_flags
reports
optional_rules_config

The Python backend must operate against these Supabase tables.

What I want built

Create a Python backend application with clear modules and endpoints.

Functional button logic to implement

Below are the frontend buttons/actions that need backend logic.

1. Create Client / Save Client

When user clicks Save Client:

Backend should:

validate input
insert a new row into companies
optionally assign advisor
optionally create company_users mapping
return created company object

Fields:

company_name
tin_or_tax_id
industry
country
financial_year
assigned_advisor_id
created_by

Create endpoint for this.

2. Edit Client

When user clicks Update Client:

Backend should:

validate permissions
update company details in Supabase
return updated company
3. Upload CSV

When user clicks Upload Transactions:

Backend should:

accept CSV file upload
validate required columns
upload original file to Supabase Storage
create an uploads record
parse the CSV
clean and transform rows
insert rows into transactions
update upload status and row_count
return summary:
rows imported
rows failed
upload id
company id

Required CSV columns:

transaction_date
description
reference
amount
currency
account_code
account_category
vendor_name
customer_name
tax_type
vat_amount
withholding_tax_amount
transaction_type

Include detailed validation errors.

4. Run Analysis

When user clicks Run Analysis:

Backend should:

fetch company transactions
fetch active rules from optional_rules_config
apply rule-based tax exposure analysis
clear old open/generated flags if needed
insert new rows into tax_risk_flags
calculate:
total flags
estimated exposure
severity counts
risk type counts
overall company risk score
return analysis summary

Implement simplified MVP rules for:

VAT risk
taxable transaction with no VAT
suspicious VAT amount
inconsistent VAT treatment
Withholding tax risk
likely WHT category with no withholding tax
suspicious WHT amount
Expense risk
unusually large expenses
potentially non-deductible expenses
duplicate expenses
Revenue risk
very large revenue transactions
missing tax classification

All outputs must be framed as:
risk indicators for advisor review, not final legal tax advice.

5. Generate Report

When user clicks Generate Report:

Backend should:

fetch company data
fetch transactions and tax flags
compute summary metrics
build a structured JSON report
insert a row into reports
return the generated report data

Include:

company info
period
total transactions analysed
total risk flags
exposure by category
severity summary
top issues
recommendations
6. Refresh Dashboard

When dashboard loads or refreshes:

Backend should return:

total transactions analysed
total flags
total estimated exposure
risk score
risk distribution
monthly exposure trend
recent alerts
recent uploads

Build:

company dashboard endpoint
advisor dashboard endpoint
7. Mark Risk as Reviewed

When user clicks Mark as Reviewed:

Backend should:

update tax_risk_flags.status = 'reviewed'
optionally store updated timestamp
return updated flag
8. Mark Risk as Resolved

When user clicks Mark as Resolved:

Backend should:

update tax_risk_flags.status = 'resolved'
return updated flag
9. View Risks with Filters

When user uses filters:

Backend should support:

filter by company
risk_type
severity
status
date range
search term
pagination

Expose endpoint with query params.

10. View Transactions with Filters

Backend should support:

company
upload_id
date range
amount range
search term
tax_type
transaction_type
pagination
sorting
11. Profile Update

When user clicks Save Changes in settings:

Backend should:

update profiles
validate allowed fields
return updated profile

Fields:

full_name
email if allowed
role only if admin
12. Logout

If backend support is needed, provide endpoint or note that frontend can use Supabase Auth logout directly.
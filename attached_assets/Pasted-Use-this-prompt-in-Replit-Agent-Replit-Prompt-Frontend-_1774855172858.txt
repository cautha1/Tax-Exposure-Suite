Use this prompt in **Replit Agent**.

---

## Replit Prompt — Frontend for Tax Exposure Intelligence Platform

You are a senior **frontend engineer, product designer, and Supabase-integrated SaaS builder**.

Build the **frontend** for a modern B2B SaaS platform called:

# Tax Exposure Intelligence Platform

This platform is for **tax advisory firms, audit firms, and finance teams**.
It helps users identify **tax exposure and compliance risks** before an audit happens.

The frontend must be:

* modern
* premium
* professional
* fintech/regtech style
* responsive
* functional
* connected to **Supabase** as the backend

The backend, database, auth, storage, and API logic are all managed by **Supabase**.
I will provide the Supabase project URL, anon key, and schema details/API keys for connection.

Do **not** build a separate backend server unless absolutely necessary.
Use **Supabase client** for data fetching, auth, storage, and RPC calls.

---

# Tech Stack

Use:

* **Next.js**
* **React**
* **TypeScript**
* **Tailwind CSS**
* **shadcn/ui**
* **Lucide icons**
* **Recharts** for analytics charts
* **Supabase JS client**

---

# Main Goal

Build a **fully functional frontend MVP** that connects to Supabase and feels like a real enterprise analytics product.

The site must not just look good — the buttons, forms, tables, uploads, filters, and navigation must all work.

Every major button should have real functionality connected to Supabase or to realistic UI flows.

---

# Product Positioning

The platform should be presented as:

**Tax Exposure Intelligence for Proactive Advisory**

It is not replacing accountants or tax advisors.
It helps them work faster using analytics.

---

# Design Direction

The UI should look like a premium SaaS dashboard used by consulting and finance firms.

Style requirements:

* white or very light background
* deep blue / indigo accents
* dark text
* clean cards
* smooth spacing
* elegant tables
* polished charts
* rounded-xl or 2xl corners
* subtle shadows
* modern sidebar and topbar
* mobile responsive
* executive-friendly

Avoid:

* cartoonish design
* too many colors
* generic student-project look
* cluttered layouts

---

# Supabase Integration Requirements

The frontend must be prepared to connect to Supabase using environment variables.

Create `.env.example` with:

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

Create a reusable Supabase client setup.

Use Supabase for:

* authentication
* reading/writing companies
* reading/writing transactions
* file upload to storage
* reading/writing tax risk flags
* reading reports
* calling Supabase RPC functions if available

Assume the tables already exist in Supabase, including:

* profiles
* companies
* company_users
* uploads
* transactions
* tax_risk_flags
* reports
* optional_rules_config

---

# User Roles

The UI should support these roles:

## Admin

Can view everything.

## Advisor

Can:

* see assigned companies
* create and manage clients
* upload company transactions
* run analysis
* view dashboards
* view reports

## Client User

Can:

* view only their company
* upload company files
* view their tax exposure dashboard
* view reports

Use role-based rendering in the UI based on profile data from Supabase.

---

# Pages To Build

Create these frontend pages:

## Public Pages

1. Landing page
2. Login page
3. Signup page

## Protected Pages

4. Dashboard
5. Clients list
6. Client detail page
7. Client upload page
8. Transactions page
9. Risks page
10. Report page
11. Settings page

Use App Router and a clean dashboard layout.

---

# Landing Page

Create a premium B2B landing page.

Sections:

## Hero

Headline:
**Tax Exposure Intelligence for Proactive Advisory**

Subheadline:
Help advisory firms and finance teams detect tax risk earlier, analyse transactions faster, and generate audit-ready tax exposure insights.

Buttons:

* Request Demo
* View Platform

The buttons should work:

* Request Demo scrolls to contact/CTA section
* View Platform goes to login or dashboard depending on auth state

## Problem Section

Explain:

* tax issues are often discovered too late
* manual spreadsheet reviews are slow
* advisory firms need analytics

## Solution Section

Explain:

* connect accounting data
* upload transactions
* detect tax exposure risks
* generate dashboards and reports

## Features Section

Show cards for:

* Multi-client dashboards
* CSV uploads
* Risk scoring
* Tax flags
* Advisory reports

## Who It’s For

* tax advisory firms
* audit firms
* finance teams
* multi-entity companies

## Final CTA

Buttons:

* Start Demo
* Sign In

Make them functional.

---

# Authentication Pages

## Login Page

Include:

* email
* password
* login button
* forgot password placeholder
* link to signup

Button must authenticate with Supabase Auth.

## Signup Page

Include:

* full name
* email
* password
* role selector (advisor or client_user, and optionally admin hidden/manual)
* signup button

On signup:

* create user in Supabase Auth
* rely on backend trigger / profile logic if available
* if needed, insert profile row after signup

Add loading and error states.

---

# Dashboard Layout

Use a professional app shell with:

## Sidebar

Links:

* Dashboard
* Clients
* Transactions
* Risks
* Reports
* Settings

Sidebar should be collapsible on smaller screens.

## Topbar

Include:

* page title
* company selector if relevant
* search placeholder
* user dropdown
* logout button

Logout button must work.

---

# Dashboard Page

Build a polished dashboard.

## Advisor Dashboard

Show:

* total clients
* total transactions analysed
* total open tax flags
* estimated exposure amount
* high-risk companies
* recent uploads
* recent alerts

## Client Dashboard

Show:

* company overview
* total transactions analysed
* risk score
* flags count
* estimated exposure
* risk breakdown
* recent flagged items

## Charts

Use Recharts for:

* risk by category
* severity breakdown
* monthly exposure trend
* flagged issues over time

Use real Supabase data where available.

If RPC/dashboard aggregation functions exist in Supabase, call them.
If not, compute summaries on the frontend using queried data.

---

# Clients Page

Create a beautiful clients management page.

Features:

* client/company cards or table
* search bar
* filters by industry or risk level
* Add Client button
* View Details button
* Edit button

## Add Client Modal / Form

Fields:

* company_name
* tin_or_tax_id
* industry
* country
* financial_year
* assigned_advisor_id if available

Buttons:

* Save Client
* Cancel

Save Client must insert into Supabase.

---

# Client Detail Page

Show:

* company information
* assigned advisor
* summary cards
* tabs for:

  * Overview
  * Uploads
  * Transactions
  * Risks
  * Reports

Buttons:

* Upload Transactions
* Run Analysis
* Generate Report

Each button must do something real:

### Upload Transactions

Go to upload page or open upload modal.

### Run Analysis

Call a Supabase RPC or backend function such as:
`analyse_company_transactions(company_id)`

If RPC is not yet available, structure the UI so the button is ready and shows a meaningful placeholder state.

### Generate Report

Call a Supabase RPC or insert into reports table using current data.

---

# Upload Page

Create a CSV upload interface.

Features:

* drag and drop area
* file picker button
* CSV template download button
* preview first rows after upload
* validation status
* upload progress
* import button

Required workflow:

1. user selects CSV
2. validate required headers
3. preview rows
4. upload file to Supabase Storage
5. insert upload record into `uploads`
6. parse rows and insert into `transactions`
7. show success message and row count

Required CSV columns:

* transaction_date
* description
* reference
* amount
* currency
* account_code
* account_category
* vendor_name
* customer_name
* tax_type
* vat_amount
* withholding_tax_amount
* transaction_type

Add friendly errors for invalid files.

---

# Transactions Page

Create a full transactions table.

Features:

* search
* filters
* pagination
* sorting
* date formatting
* currency formatting
* badge for tax type
* link back to source upload if possible

Columns:

* date
* description
* reference
* amount
* currency
* account category
* vendor/customer
* tax type
* VAT
* WHT
* transaction type

Use Supabase queries with pagination or limit/offset patterns.

---

# Risks Page

Create a risks table with strong UX.

Features:

* filters by severity
* filters by risk type
* search
* status tabs
* sort by exposure or score
* badge colors for severity
* open/reviewed/resolved states

Columns:

* date
* issue title
* risk type
* severity
* transaction
* estimated exposure
* recommendation
* status

Buttons:

* Mark as Reviewed
* Mark as Resolved
* View Transaction

These buttons should update Supabase.

---

# Reports Page

Create a report interface for each company.

Report sections:

* company overview
* reporting period
* total transactions analysed
* total flags
* estimated exposure
* exposure by category
* key findings
* recommended next steps
* flagged transactions snapshot

Buttons:

* Refresh Report
* Print Report
* Export PDF-ready view

Use a polished layout suitable for client presentation.

If no PDF export utility is added yet, create a print-friendly page and a clear placeholder for export.

---

# Settings Page

Create a settings page with:

* profile details
* name
* email
* role
* company associations
* logout
* future-ready preferences section

Buttons:

* Update Profile
* Save Changes
* Logout

Use Supabase update operations where appropriate.

---

# Functional Button Requirements

This is very important:

Do not create dead buttons.

Every major button must either:

* perform a real Supabase action
* navigate correctly
* open a working modal
* call a real handler
* display a valid loading/error/success state

Examples:

* Login button authenticates
* Signup button registers
* Save Client inserts to Supabase
* Upload button stores file and imports data
* Run Analysis triggers analysis flow
* Generate Report triggers report creation
* Mark Reviewed updates flag status
* Logout ends session

---

# Components To Build

Create reusable components:

* AppSidebar
* TopNavbar
* PageHeader
* KPIStatCard
* ChartCard
* CompanyCard
* CompanyTable
* RiskBadge
* SeverityBadge
* StatusBadge
* UploadDropzone
* CSVPreviewTable
* TransactionsTable
* RisksTable
* ReportSummaryCard
* EmptyState
* LoadingState
* ErrorState
* ProtectedRoute/AuthGuard

---

# Data and State Handling

Use clean patterns for:

* loading states
* optimistic UI where safe
* empty states
* error handling
* role-based rendering
* reusable typed data models

Use TypeScript types for:

* Profile
* Company
* Upload
* Transaction
* TaxRiskFlag
* Report

---

# Folder Structure

Use a clean structure like:

```text
app/
  (auth)/
  (dashboard)/
components/
  ui/
  dashboard/
  tables/
  charts/
  forms/
lib/
  supabase/
  utils/
  actions/
types/
public/
```

Include:

* Supabase client files
* route guards
* data helpers
* formatting helpers

---

# Nice-to-Have Features

If time allows, also include:

* toast notifications
* empty chart states
* company switcher
* recent activity section
* responsive mobile sidebar
* dark-mode-ready structure
* seeded mock fallback only for non-critical preview UI, but core data must use Supabase

---

# Important Implementation Notes

* Make the platform feel real and demo-ready
* Keep the UI polished and premium
* Prioritize working flows
* Connect as much as possible to Supabase
* Use environment variables for Supabase credentials
* Make the code easy to extend later
* Add comments where helpful
* Ensure responsive design

---

# Final Output Required

Generate the frontend codebase with:

1. all pages
2. modern dashboard UI
3. Supabase connection setup
4. functional auth flow
5. clients CRUD UI
6. upload UI and import flow
7. transactions table
8. risks management UI
9. report page
10. settings page
11. reusable components
12. `.env.example`
13. README with setup instructions

Do not just outline the design.
Actually generate the working frontend code and structure.

Make the result look like a **real fintech/regtech platform** that I can connect to my Supabase project and demo professionally.

---

## Shorter Replit version

If Replit Agent responds better to a shorter prompt, use this:

Build a premium **Next.js + TypeScript + Tailwind + shadcn/ui** frontend for a B2B SaaS product called **Tax Exposure Intelligence Platform**. Connect it to **Supabase** for auth, database, storage, and RPC. Create a modern fintech/regtech UI with a landing page, login, signup, dashboard, clients, client detail, upload, transactions, risks, reports, and settings pages. Make the buttons functional: login/signup with Supabase Auth, create clients in Supabase, upload CSVs to Supabase Storage, save upload metadata, import transactions, update risk flag status, generate reports, and logout. Use role-based rendering for admin, advisor, and client users. Use Recharts for dashboards. Add reusable components, loading/error states, `.env.example`, and a clean app structure. Make it polished, modern, responsive, and demo-ready.

---

After Replit generates it, send me the result and I’ll help you fix the prompt further or tighten the UI and Supabase connection.
